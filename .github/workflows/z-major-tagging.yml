name: major-tagging

on:
  push:
    tags:
      - v**

permissions:
  contents: write

jobs:
  tag:
    name: Move major tag
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: phwt/major-tagging-action@v1
